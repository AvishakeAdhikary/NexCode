<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="NexCode.Gui.Pages.Settings.AutomationsSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:NexCode.Gui.ViewModels.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="6">
            <TextBlock
                FontSize="24"
                FontWeight="SemiBold"
                Foreground="{StaticResource NexCode.Text.Primary}"
                Text="Automations" />
            <TextBlock
                Foreground="{StaticResource NexCode.Text.Secondary}"
                Text="Run on triggers (manual, cron, file-watch, hook). Detailed step builder lives on the Automations nav-rail page." />
        </StackPanel>

        <Border
            Grid.Row="1"
            Padding="0"
            Background="{StaticResource NexCode.Surface.Card}"
            BorderBrush="{StaticResource NexCode.Border.Subtle}"
            BorderThickness="1"
            CornerRadius="12">
            <ListView
                x:Name="AutomationsListView"
                ItemsSource="{x:Bind ViewModel.Automations, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedAutomation, Mode=TwoWay}"
                SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:AutomationRowViewModel">
                        <Grid Padding="12,10" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind Name, Mode=OneWay}" />
                                <TextBlock
                                    FontSize="12"
                                    Foreground="{StaticResource NexCode.Text.Secondary}"
                                    Text="{x:Bind StepsSummary, Mode=OneWay}" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{x:Bind TriggerSummary, Mode=OneWay}" />
                            <TextBlock Grid.Column="2" Text="{x:Bind ScheduleCron, Mode=OneWay}" />
                            <StackPanel
                                Grid.Column="3"
                                Orientation="Horizontal"
                                Spacing="4">
                                <Button
                                    Command="{Binding ElementName=AutomationsListView, Path=DataContext.RunCommand}"
                                    CommandParameter="{x:Bind}"
                                    Content="Run" />
                                <Button
                                    Command="{Binding ElementName=AutomationsListView, Path=DataContext.ToggleCommand}"
                                    CommandParameter="{x:Bind}"
                                    Content="Toggle" />
                                <Button
                                    Command="{Binding ElementName=AutomationsListView, Path=DataContext.DeleteCommand}"
                                    CommandParameter="{x:Bind}"
                                    Content="Delete" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Foreground="{StaticResource NexCode.Text.Secondary}"
                Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                VerticalAlignment="Center" />
            <Button
                Grid.Column="1"
                Command="{x:Bind ViewModel.AddAutomationCommand}"
                Content="Add automation" />
        </Grid>
    </Grid>
</Page>
