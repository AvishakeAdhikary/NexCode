<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="NexCode.Gui.Pages.Settings.SshKeysSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="6">
            <TextBlock
                FontSize="24"
                FontWeight="SemiBold"
                Foreground="{StaticResource NexCode.Text.Primary}"
                Text="SSH keys" />
            <TextBlock
                Foreground="{StaticResource NexCode.Text.Secondary}"
                Text="Used by remote workspaces. Private keys live in DPAPI-protected storage." />
        </StackPanel>

        <Border
            Grid.Row="1"
            Padding="16"
            Background="{StaticResource NexCode.Surface.Card}"
            BorderBrush="{StaticResource NexCode.Border.Subtle}"
            BorderThickness="1"
            CornerRadius="12">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="Generate Ed25519" />
                <Button Content="Generate RSA 4096" />
                <Button Content="Import .pub" />
            </StackPanel>
        </Border>

        <Border
            Grid.Row="2"
            Padding="16"
            Background="{StaticResource NexCode.Surface.Card}"
            BorderBrush="{StaticResource NexCode.Border.Subtle}"
            BorderThickness="1"
            CornerRadius="12">
            <ListView SelectionMode="Single">
                <ListViewItem Content="No SSH keys yet — generate or import to begin." IsHitTestVisible="False" />
            </ListView>
        </Border>
    </Grid>
</Page>
