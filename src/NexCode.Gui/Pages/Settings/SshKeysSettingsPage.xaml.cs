using Microsoft.UI.Xaml.Controls;

namespace NexCode.Gui.Pages.Settings;

/// <summary>SSH key registry panel (spec §32). Generation/import are wire-up pending.</summary>
public sealed partial class SshKeysSettingsPage : Page
{
    public SshKeysSettingsPage()
    {
        InitializeComponent();
    }
}
