using Microsoft.UI.Xaml.Controls;

namespace NexCode.Gui.Pages.Settings;

/// <summary>Default shell selector (spec §32).</summary>
public sealed partial class ShellSettingsPage : Page
{
    public ShellSettingsPage()
    {
        InitializeComponent();
    }
}
