<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="NexCode.Gui.Controls.JsonSchemaEditorSheet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:NexCode.Gui.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock x:Name="TitleTextBlock" FontWeight="SemiBold" FontSize="16" />
            <TextBlock x:Name="SchemaIdTextBlock" Opacity="0.65" />
        </StackPanel>

        <controls:MonacoHost x:Name="EditorHost" Grid.Row="1" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button x:Name="ValidateButton" Content="Validate" Click="ValidateButton_Click" />
            <Button x:Name="SaveButton" Content="Save" Click="SaveButton_Click" Style="{ThemeResource AccentButtonStyle}" />
            <Button x:Name="CloseButton" Content="Close" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
